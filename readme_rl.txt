# readme of RL
# tools&package  need to be installed to run the code of reinforcement learning

1. python2.7
2. pip tools # https://pip.pypa.io/en/stable/installing/ visiting this website
3. tensorflow #https://www.tensorflow.org/install/pip?lang=python2 visiting this page
	note: python3.7 will not be supportted by tensorflow, you should uninstall python3.7 and install python 3.6 again
4. tar decompress tools #www.rarlab.com/download.htm
5. gym  #pip install gym
5. cv2 #sudo pip2 install opencv-python
6. pillow #sudo pip2 install pillow
7. ImportError: libSm.so.6: cannot open shared object file # and branch of related
   issues
   sudo apt-get install libm6
   sudo apt-get install libxrender1
   sudo apt-get install libxext-dev